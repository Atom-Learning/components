---
title: Toggle Field
component: ToggleField
description: Combines a Toggle with a Label
category: Forms
---

`ToggleField` wraps a `Toggle` with an inline `Label` on the left side.

```tsx preview
<Form>
  <ToggleField label="Example Toggle Field"/>
</Form>
```

