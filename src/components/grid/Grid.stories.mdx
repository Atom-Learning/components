import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks'

import { Grid } from '.'
import { Box } from '../'

<Meta title="Grid" component={Grid} />

# Grid

The `Grid` component is analagous to `Box` but additionally sets the rendering context of its children to `display: grid`.

<Canvas>
  <Story name="Basic">
    <Grid
      css={{
        gap: '$3',
        gridAutoFlow: 'column'
      }}
    >
      <Box css={{ bg: '$tonal200', p: '$2' }}>Hello</Box>
      <Box css={{ bg: '$tonal200', p: '$2' }}>Hello</Box>
    </Grid>
  </Story>
</Canvas>

## Props

<ArgsTable of={Grid} />
