import { CheckboxItem } from '@radix-ui/react-dropdown-menu'

import { styled } from '~/stitches'

import { itemStyles } from './DropdownMenuItem'

export const DropdownMenuCheckboxItem = styled(CheckboxItem, itemStyles)
