import {
  Canvas,
  Meta,
  Story,
  ArgsTable,
  Preview
} from '@storybook/addon-docs/blocks'

import { Button } from './'
import { Flex, Grid } from '~/components/'

import { disableStitchesProps } from '~/utilities/storybook'

<Meta
  title="Button"
  component={Button}
  argTypes={{
    theme: 'string',
    variant: 'string',
    ...disableStitchesProps
  }}
/>

# Button

The `Button` component is a very thin wrapper around an HTML `button` element. It adds default styling and the `css` prop. By default `primary` theme is displayed with a `solid` appearance.

<Preview>
  <Story name="Basic">
    <Button css={{ mb: '$2' }}>Primary</Button>
  </Story>
</Preview>

## Themes

These are the available `themes` for the `Button` component: `Primary` (default), `Secondary`, `Tertiary`, `Success`, `Warning`, `Danger`

<Preview>
  <Story name="Themes">
    <Flex
      css={{
        flexDirection: 'row',
        justifyContent: 'space-between',
        flexWrap: 'wrap'
      }}
    >
      <Button css={{ mb: '$2' }}>Primary</Button>
      <Button css={{ mb: '$2' }} theme="secondary">
        Secondary
      </Button>
      <Button css={{ mb: '$2' }} theme="tertiary">
        Tertiary
      </Button>
      <Button css={{ mb: '$2' }} theme="success">
        Success
      </Button>
      <Button css={{ mb: '$2' }} theme="warning">
        Warning
      </Button>
      <Button css={{ mb: '$2' }} theme="danger">
        Error
      </Button>
    </Flex>
  </Story>
</Preview>

## Appearearce

There two options for the `appearance` property: `solid` and `outline`.

These are the available `outline` variations for the `primary`, `secondary` and `tertiary` themes.

<Preview>
  <Story name="Outline">
    <Flex
      css={{
        flexDirection: 'row',
        justifyContent: 'space-between',
        width: '500px'
      }}
    >
      <Button css={{ mb: '$2' }} appearance="outline">
        Primary
      </Button>
      <Button css={{ mb: '$2' }} appearance="outline" theme="secondary">
        Secondary
      </Button>
      <Button css={{ mb: '$2' }} appearance="outline" theme="tertiary">
        Tertiary
      </Button>
    </Flex>
  </Story>
</Preview>

### Disabled state

Each variation has an `disabled` stated, by setting the `disabled` property.

<Preview>
  <Story name="Disabled">
    <Flex
      css={{
        flexDirection: 'row',
        justifyContent: 'space-between',
        flexWrap: 'wrap'
      }}
    >
      <Button css={{ mb: '$2' }} disabled>
        Disabled button
      </Button>
      <Button css={{ mb: '$2' }} appearance="outline" disabled>
        Disabled button
      </Button>
      <Button
        css={{ mb: '$2' }}
        appearance="outline"
        theme="secondary"
        disabled
      >
        Disabled button
      </Button>
      <Button css={{ mb: '$2' }} appearance="outline" theme="tertiary" disabled>
        Disabled button
      </Button>
    </Flex>
  </Story>
</Preview>

## Props

<ArgsTable of={Button} />
