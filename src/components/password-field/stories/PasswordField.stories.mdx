import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks'

import { PasswordField } from '../'

<Meta title="Components/PasswordField" component={PasswordField} />

# PasswordField

The `PasswordField` component combines an `Input` and `label` to create a standardised way of asking for a user's password.

<Canvas>
  <Story name="Password">
    <PasswordField name="pass" css={{ width: '250px' }} />
  </Story>
</Canvas>

## Props

<ArgsTable of={PasswordField} />
