import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks'

import { Button } from '../'
import { Flex } from '../../'

import { disableStitchesProps } from '../../../utilities/storybook'

<Meta
  title="Primitives/Button"
  component={Button}
  argTypes={{
    variant: 'string'
  }}
/>

# Button

The `Button` component is a very thin wrapper around an HTML `button` element. It adds default styling and the `css` prop.

<Canvas>
  <Story name="Basic">
    <Flex
      css={{
        borderWidth: 1,
        flexDirection: 'column',
        justifyContent: 'flex-start',
        width: '200px',
        height: '500px'
      }}
    >
      <Button css={{ mb: '$2' }}>Primary</Button>
      <Button css={{ mb: '$2' }} variant={'secondary'}>
        Secondary
      </Button>
      <Button css={{ mb: '$2' }} variant={'tertiary'}>
        Tertiary
      </Button>
    </Flex>
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />
