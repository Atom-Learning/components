import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks'

import { Flex } from '../'

<Meta title="Primitives/Flex" component={Flex} />

# Flex

The `Flex` component builds on top of `Box` to apply flex styles to a container component.

<Canvas>
  <Story name="Basic">
    <Flex
      as="aside"
      css={{
        backgroundColor: '$primary500',
        color: 'white',
        justifyContent: 'space-between',
        p: '$3'
      }}
    >
      <Box>Hello from the left!</Box>
      <Box>Hello from the right!</Box>
    </Flex>
  </Story>
</Canvas>

## Props

<ArgsTable of={Flex} />
