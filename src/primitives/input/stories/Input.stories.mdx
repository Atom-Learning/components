import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks'

import { Input } from '../'
import { Flex } from '../../'

<Meta title="Primitives/Input" component={Input} />

# Input

The `Input` component is a very thin wrapper around an HTML `input` element. It handles some basic styling and some UX / accessibility concerns for number inputs.

<Canvas>
  <Story name="Basic">
    <Flex
      css={{
        flexDirection: 'column',
        justifyContent: 'space-between',
        width: '200px',
        height: '200px'
      }}
    >
      <Input name="example" placeholder="example" />
      <Input type="number" name="number-example" placeholder="001" />
    </Flex>
  </Story>
</Canvas>

## Props

<ArgsTable of={Input} />
