import { ComboboxList as BaseComboboxList } from '@reach/combobox'

import { styled } from '~/stitches'

export const ComboboxFieldList = styled(BaseComboboxList, {
  listStyle: 'none',
  m: 0,
  p: 0,
  userSelect: 'none'
})
